# [專案名稱] — UI 功能脈絡 (ui-context.md 範例)

> 💡 **範例說明**：這是一份前端專案 `ui-context.md` 的撰寫範例。本文件旨在定義頁面的功能、狀態、權限限制與錯誤處理，作為 AI 測試腳本生成 (E2E) 或開發實作的重要參考依據。請在建立新專案時，複製此格式並依實際狀況填寫。
>
> 搭配閱讀：[README.md](./README.md)（技術棧與頁面清單）
> ⚠️ 此文件由 AI 產出，需資深工程師審核後生效

---

## 使用者角色

| 角色 | 可存取範圍 | 限制 |
|------|-----------|------|
| [角色A，例如：一般玩家] | [例如：所有前台功能] | [例如：不可進入後台管理] |
| [角色B，例如：管理員] | [例如：所有功能，包含後台管理區塊] | 無 |

> 註：此處的角色定義主要影響前端 UI 的顯示與隱藏（如選單按鈕），實際寫入/讀取權限需由後端 API 進行控制。

---

## 頁面功能描述

### [頁面名稱，例如：登入頁面] `[路由路徑，例如：/login]`

**路由**：`[路由路徑]`
**對應 API**：
- `[HTTP 方法] [API 路徑]` → [說明，例如：登入驗證]
- `[HTTP 方法] [API 路徑]` → [說明，例如：取得使用者資訊]

**這個頁面能做什麼**：
- [操作說明 1，例如：輸入公司代號、帳號、密碼進行登入]
- [操作說明 2，例如：切換顯示語言（繁體中文、英文）]

**功能限制**：

| 限制條件 | 使用者看到的行為 |
|---------|---------------|
| [例如：任一欄位空白時按下登入] | [例如：顯示紅框提示「請輸入完整資料」（透過 Toast 顯示）] |
| [例如：登入失敗（帳密錯誤）] | [例如：顯示後端回傳的錯誤訊息或「系統異常」] |
| [例如：已登入狀態下進入此頁] | [例如：自動導向 `/home`] |

**狀態說明**：

[例如：按鈕在 API 請求期間應顯示 Loading 狀態，且不可重複點擊。]

**錯誤處理**：

| 錯誤情境 | 使用者看到的行為 |
|---------|---------------|
| [例如：網路斷線或 5xx] | [例如：Toast 顯示「系統異常，請稍後再試」] |
| [例如：後端回傳 401] | [例如：清除 Token 並導向登入頁] |

---

### [頁面名稱，例如：首頁] `[路由路徑，例如：/home]`

**路由**：`[路由路徑]`
**對應 API**：
- [API 清單]

**這個頁面能做什麼**：
- [操作說明清單]

**功能限制**：
- [限制條件與行為清單]

**(依此類推，為專案的各個主要頁面建立對應的區塊)**