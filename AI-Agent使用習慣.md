# 節省 Token 的好習慣

---

## 丟掉用不到的 Context

1. 開啟新視窗
2. 打錯字不要讓 AI 輸出，修改原字樣或停止
3. 精簡掛載工作或 MCP

---

## 縮減留下來的 Context

1. 萃取出關鍵部分（例：PDF 等等，先整理出關鍵再餵給 AI）
2. 只給結論不交草稿（之前討論的過程，整理成一份 `.md`，再開新視窗給它）
3. 可調整的情況下，Input 盡量只給 Text 或 Code
4. 同一段程式碼／文件若需一再引用，改用檔案路徑或 reference，不要每次整段重貼
5. 能給文字／OCR／錯誤訊息原文，就不要貼截圖（圖片 token 數遠高於文字）
6. Few-shot 範例要精簡，只留必要示範
7. 定期讓 AI「摘要到目前為止」，把摘要當作新起點繼續

---

## 縮減 Output

1. 明確指定輸出格式（例：只要 diff、不要解釋；先給結論再展開）
2. 只輸出改動的段落，不要一直重新 Output 完整段落
3. 要求「有問題再問，不要重講背景」

---

## 利用折扣機制

1. 利用 AI 快取 Prompt Caching，但快取壽命因平台而異，約為 5 分鐘 ~ 1 小時，所以盡量不要空著對話框超過快取壽命，以下為各平台參考:

- Claude: 預設 5 分鐘、可調到最多 1 小時
- Gemini: 預設 1 小時
- OpenAI: 自動快取約 5～10 分鐘

2. 不變的內容（AGENT.md、系統提示、常用程式碼）放對話最前面，讓快取更容易命中
3. 不要破壞快取（例：中途換模型、中途調整思考模式、中途開加速）
4. 若沒必要，關閉不需要的功能（Web Search、推理／思考顯示、加速等）也可省 Token

---

## 其他

1. System prompt 精簡：固定規則放到專案 rules／`AGENT.md`，別每輪重貼
2. 定期看 Token 用量報告，找出真正浪費的地方

---

## 重點

1. `AGENT.md` 也能夠精簡化
2. 每次對話問自己 AI 回答我的東西跟上一個有沒有關係，無關就換視窗
3. 若有關，就將結論整理成一份 `.md` 送去新視窗
4. 開新視窗是為了丟掉「不再需要」的舊資料；但若這個視窗還要用，就別放超過快取 TTL，否則白白重付舊資料的錢

**AI 聽你說比較便宜，你聽 AI 說比較貴**
**用得兇不代表花得冤枉，每一輪都付一次舊資料的錢才冤枉**
